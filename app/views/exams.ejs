<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="utf-8">
        <meta name="viewport" content="width=device-width, initial-scale=1">
        <title>home</title>
        <script type="text/javascript" src="jq.js"></script>
        <link rel="stylesheet" href="bootstrap.css" type="text/css">
        <style>
            .text-small {
                font-size: 85%;
            }

            .dropdown-toggle:not(:focus) {
                outline: 0;
            }
            .log-out-a {
                color: inherit;
                text-decoration: none;
            }
            .home-wrapper {
                width: 1000px;
                min-height: 700px;
                margin: 0 auto;
            }
            .home-section { 
                margin-bottom: 20px;
            }
            .photo {
                width: 300px;
                height: 200px;
                margin-bottom: 22px;
            }
            .empty-data {
                width: 1000px;
                height: 700px;
                text-align: center;
                align-items: center;
                align-content: center;
            }
        </style>
    </head>
    <body>
        <header class="p-3 mb-3 border-bottom">
            <div class="container">
                <div class="d-flex flex-wrap align-items-center justify-content-center justify-content-lg-start">
                    <ul class="nav col-12 col-lg-auto me-lg-auto mb-2 justify-content-center mb-md-0">
                        <li><a href="/home" class="nav-link px-2 link-body-emphasis">Home</a></li>
                        <% if (student.can_see_exams) { %> 
                            <li><a href="/exams" class="nav-link px-2 link-secondary">Exams</a></li>
                        <% } %>
                    </ul>
            
                    <form id="search-form" class="col-12 col-lg-auto mb-3 mb-lg-0 me-lg-3" role="search">
                        <input id="search-input" type="search" class="form-control" placeholder="Search..." aria-label="Search">
                    </form>
            
                    <div class="text-end">
                        <a href="/logout" class="btn btn-primary">Log Out</a>
                    </div>
                </div>
            </div>
        </header>
        <main>
            <section id="home-section" class="home-section">
                <div class="home-wrapper">
                    <% if (data.length == 0) { %> 
                        <div class="empty-data">
                            <img class="photo" src="empty.png"/>
                            <p>There is no exams for you!</p>
                        </div>
                      <% } else { %> 
                            <% for(let i = 0; i < data.length; i++) {%>
                                <div class="card w-80 mt-4">
                                    <div class="card-body d-flex flex-row">
                                        <h5 class="card-title"><%=data[i].name%></h5>
                                        <button type="button" class="btn btn-success btn-sm" style="margin-left: auto;"><%=data[i].mark%></button>
                                    </div>
                                </div>
                            <%}%>
                      <% } %>
                </div>
            </section>
        </main>
        <footer class="d-flex flex-wrap justify-content-between align-items-center py-3 my-4 border-top">
            <p class="col-md-4 mb-0 text-muted" style="margin-left: 32px;">&copy; 2024 Sergeev Danila</p>
            <ul class="nav col-md-4 justify-content-end" style="margin-right: 32px;">
              <li class="nav-item"><a href="/home" class="nav-link px-2 text-muted">Home</a></li>
              <% if (student.can_see_exams) { %> 
                <li class="nav-item"><a href="/exams" class="nav-link px-2 text-muted">Exams</a></li>
            <% } %>
            </ul>
        </footer>
    </body>
</html>
