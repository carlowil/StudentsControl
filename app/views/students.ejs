<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="utf-8">
        <meta name="viewport" content="width=device-width, initial-scale=1">
        <title>Admin Panel</title>
        <script type="text/javascript" src="/jq.js"></script>
        <script type="text/javascript" src="/check-switch.js"></script>
        <link rel="stylesheet" href="/bootstrap.css" type="text/css">
        <style>
            .text-small {
                font-size: 85%;
            }
            .dropdown-toggle:not(:focus) {
                outline: 0;
            }
            .log-out-a {
                color: inherit;
                text-decoration: none;
            }
            main {
                width: 1000px;
                margin: 0 auto;
                min-height: 700px;
            }
            .empty-data {
                width: 700px;
                height: 700px;
                text-align: center;
                align-items: center;
                align-content: center;
            }
        </style>
    </head>
    <body>
        <header class="p-3 mb-3 border-bottom">
            <div class="container">
                <div class="d-flex flex-wrap align-items-center justify-content-center justify-content-lg-start">
                    <ul class="nav col-12 col-lg-auto me-lg-auto mb-2 justify-content-center mb-md-0">
                        <li><a href="/professor-panel/tasks" class="nav-link px-2 link-body-emphasis">Tasks</a></li>
                        <li><a href="/professor-panel/students" class="nav-link px-2 link-secondary">Students</a></li>
                    </ul>
            
                    <form id="search-form" class="col-12 col-lg-auto mb-3 mb-lg-0 me-lg-3" role="search" style="display: flex;">
                        <input id="search-input" type="search" class="form-control" placeholder="Search..." aria-label="Search">
                    </form>
            
                    <div class="text-end">
                        <a href="/logout" class="btn btn-primary">Log Out</a>
                    </div>
                </div>
            </div>
        </header>
        <main>
            <% if (data.length == 0) { %> 
                <div class="empty-data">
                    <img class="photo" src="/empty.png"/>
                    <p>There is no students</p>
                </div>
              <% } else { %> 
                    <% for(let i = 0; i < data.length; i++) {%>
                        <% if (data[i].role == 'student') { %> 
                            <div class="card w-80 mt-4" data-id="<%=data[i].id%>">
                                <h5 class="card-header"><%=data[i].username%></h5>
                                <div class="card-body d-flex flex-row">
                                    <h5 class="card-title">Student id: <%=data[i].id%>.</h5>
                                    <div class="div-options d-flex flex-row" style="margin-left: auto;">
                                        <div class="form-check form-switch" style="margin-right: 20px;">
                                            <% if (data[i].can_see_marks) { %>
                                                <input class="form-check-input flexCheckMarks" type="checkbox" checked>
                                            <% } else { %> 
                                                <input class="form-check-input flexCheckMarks" type="checkbox">
                                            <% } %>
                                            <label class="form-check-label" for="flexCheckMarks">Can see marks?</label>
                                        </div>
                                        <div class="form-check form-switch">
                                            <% if (data[i].can_see_exams) { %>
                                                <input class="form-check-input flexCheckExams" type="checkbox" checked>
                                            <% } else { %> 
                                                <input class="form-check-input flexCheckExams" type="checkbox">
                                            <% } %>
                                            <label class="form-check-label" for="flexCheckMarks">Can see exams?</label>
                                        </div>
                                    </div>
                                </div>
                            </div>
                          <% } %> 
                    <%}%>
              <% } %>
        </main>
        <footer class="d-flex flex-wrap justify-content-between align-items-center py-3 my-4 border-top">
            <p class="col-md-4 mb-0 text-muted" style="margin-left: 32px;">&copy; 2024 Sergeev Danila</p>
            <ul class="nav col-md-4 justify-content-end" style="margin-right: 32px;">
              <li class="nav-item"><a href="/professor-panel/tasks" class="nav-link px-2 text-muted">Tasks</a></li>
              <li class="nav-item"><a href="/professor-panel/students" class="nav-link px-2 text-muted">Students</a></li>
            </ul>
        </footer>
    </body>
</html>